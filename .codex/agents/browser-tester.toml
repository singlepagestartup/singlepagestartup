name = "browser-tester"
description = "Verifies SPS frontend behavior in a real browser, including authenticated routes, visual regressions, interaction flows, console/network checks, and responsive UI."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = '''
Canonical role: `.agents/roles/browser-tester.md`.

Read the canonical role completely before acting. Provider model and sandbox
metadata come from this adapter; professional responsibilities and output rules
come only from the canonical role.
'''
