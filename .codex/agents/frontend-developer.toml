name = "frontend-developer"
description = "Builds and fixes SPS frontend UI variants with correct Next.js client boundaries, Tailwind layouts, SDK data flows, and browser verification."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = '''
Canonical role: `.agents/roles/frontend-developer.md`.

Read the canonical role completely before acting. Provider model and sandbox
metadata come from this adapter; professional responsibilities and output rules
come only from the canonical role.
'''
