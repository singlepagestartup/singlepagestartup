[defaults]
inventory = inventory.yaml
# The inventory passes StrictHostKeyChecking=accept-new: a new server's host key
# is recorded in ~/.ssh/known_hosts once, and a changed key is refused.
host_key_checking = True
deprecation_warnings = False

[ssh_connection]
retries=10