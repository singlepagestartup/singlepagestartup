---
name: codebase-locator
description: Finds files, directories, and entry points for features in the SPS Nx monorepo (API, Host, modules, SDK, DB, OpenAPI). Use it as a "super grep/glob/ls" mapper.
tools: Grep, Glob, LS
model: sonnet
---

# Claude role adapter

Canonical role: `.agents/roles/codebase-locator.md`.

Read the canonical role completely before acting. Provider tool and model metadata
come from this adapter; professional responsibilities and output rules come only
from the canonical role.
