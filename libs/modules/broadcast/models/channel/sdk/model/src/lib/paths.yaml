paths:
  /broadcast/channels:
    get:
      tags:
        - "broadcast-channel"
      summary: Get all entities
      description: Retrieve a list of all entities with optional filtering, sorting and pagination
      parameters:
        - $ref: "../../../../../../../../../libs/shared/backend/api/src/lib/query-builder/offset.yaml"
        - $ref: "../../../../../../../../../libs/shared/backend/api/src/lib/query-builder/limit.yaml"
        - $ref: "../../../../../../../../../libs/shared/backend/api/src/lib/query-builder/order-by.yaml"
        - $ref: "../../../../../../../../../libs/shared/backend/api/src/lib/query-builder/filters.yaml"
        - $ref: "../../../../../../../../../libs/shared/backend/api/src/lib/middleware/no-cache.yaml"
      responses:
        "200":
          description: List of entities
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "broadcast-channel.yaml"

    post:
      tags:
        - "broadcast-channel"
      summary: Create a new entity
      description: Create a new entity
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                data:
                  $ref: "broadcast-channel-input.yaml"
              required:
                - data
      responses:
        "201":
          description: Created successfully
          content:
            application/json:
              schema:
                $ref: "broadcast-channel.yaml"

  /broadcast/channels/{id}:
    get:
      tags:
        - "broadcast-channel"
      summary: Get by ID
      description: Retrieve a specific entity by its ID
      parameters:
        - $ref: "../../../../../../../../../libs/shared/backend/api/src/lib/query-builder/id.yaml"
        - $ref: "../../../../../../../../../libs/shared/backend/api/src/lib/middleware/no-cache.yaml"
      responses:
        "200":
          description: Found successfully
          content:
            application/json:
              schema:
                $ref: "broadcast-channel.yaml"

    patch:
      tags:
        - "broadcast-channel"
      summary: Update entity
      description: Update a specific entity
      parameters:
        - $ref: "../../../../../../../../../libs/shared/backend/api/src/lib/query-builder/id.yaml"
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                data:
                  $ref: "broadcast-channel-input.yaml"
              required:
                - data
      responses:
        "200":
          description: Updated successfully
          content:
            application/json:
              schema:
                $ref: "broadcast-channel.yaml"

    delete:
      tags:
        - "broadcast-channel"
      summary: Delete entity
      description: Delete a specific entity
      parameters:
        - $ref: "../../../../../../../../../libs/shared/backend/api/src/lib/query-builder/id.yaml"
      responses:
        "204":
          description: Deleted successfully
          content:
            application/json:
              schema:
                $ref: "broadcast-channel.yaml"

  /broadcast/channels/push-message:
    post:
      tags:
        - "broadcast-channel"
      summary: Push message
      description: Push a message to broadcast channels
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                data:
                  type: string
                  description: JSON string
                  example: '{}'
              required:
                - data
      responses:
        "200":
          description: Broadcast channel
          content:
            application/json:
              schema:
                $ref: "broadcast-channel.yaml"

  /broadcast/channels/{id}/messages:
    get:
      tags:
        - "broadcast-channel"
      summary: List messages
      description: Retrieve messages for a channel
      parameters:
        - $ref: "../../../../../../../../../libs/shared/backend/api/src/lib/query-builder/id.yaml"
      responses:
        "200":
          description: Messages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "../../../../../../../../../libs/modules/broadcast/models/message/sdk/model/src/lib/broadcast-message.yaml"

    post:
      tags:
        - "broadcast-channel"
      summary: Create message
      description: Create a message for a channel
      parameters:
        - $ref: "../../../../../../../../../libs/shared/backend/api/src/lib/query-builder/id.yaml"
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                data:
                  type: string
                  description: JSON string
                  example: '{}'
              required:
                - data
      responses:
        "200":
          description: Messages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "../../../../../../../../../libs/modules/broadcast/models/message/sdk/model/src/lib/broadcast-message.yaml"

  /broadcast/channels/{id}/messages/{messageId}:
    delete:
      tags:
        - "broadcast-channel"
      summary: Delete message
      description: Delete a message for a channel
      parameters:
        - $ref: "../../../../../../../../../libs/shared/backend/api/src/lib/query-builder/id.yaml"
        - name: messageId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Messages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "../../../../../../../../../libs/modules/broadcast/models/message/sdk/model/src/lib/broadcast-message.yaml"
