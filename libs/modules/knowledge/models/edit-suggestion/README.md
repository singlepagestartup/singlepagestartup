# Knowledge Edit Suggestion

Pending markdown create/update suggestion.

Approval applies `proposedDescription` to a target `knowledge/document.description` or creates a new document, then reindexes the affected document. Rejection updates suggestion status without changing document content.
