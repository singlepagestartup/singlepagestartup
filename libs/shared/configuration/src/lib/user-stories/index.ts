import { userStory as subjectCreateOrder } from "./subject-create-order";
import { userStory as subjectAuthentication } from "./subject-authentication";

export const userStories = {
  subjectCreateOrder,
  subjectAuthentication,
};
