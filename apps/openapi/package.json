{
  "name": "openapi",
  "version": "1.0.0",
  "scripts": {
    "bundle": "npx @redocly/cli bundle ./openapi.yaml -o ./public/openapi-bundled.yaml",
    "dev": "bun run bundle && bun run --watch server.ts",
    "start": "bun server.ts"
  },
  "dependencies": {
    "hono": "^4.6.17"
  }
}
