{
  "name": "openapi",
  "version": "1.0.0",
  "scripts": {
    "build:docs": "npx @redocly/cli build-docs ./openapi.yaml --output ./dist/index.html && node ./scripts/localize-redoc.mjs",
    "build": "bun run build:docs",
    "dev": "bun run build:docs && bun run --watch server.ts",
    "start": "bun server.ts"
  },
  "dependencies": {
    "hono": "^4.6.17"
  }
}
