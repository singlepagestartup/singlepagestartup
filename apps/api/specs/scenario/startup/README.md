# Startup Scenario Namespace

Place startup-scoped issue scenarios in this folder using:

- `issue-<number>/backend-*.scenario.spec.ts`
- `issue-<number>/frontend-*.scenario.spec.tsx`

Example:

```text
apps/api/specs/scenario/startup/issue-200/
```
