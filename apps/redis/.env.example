COMPOSE_PROJECT_NAME=singlepagestartup
REDIS_DATABASES=16
REDIS_PASSWORD=password
REDIS_PORT=6379
# Memory budget for the local cache instance. Redis evicts at this limit
# instead of growing until the host kills it (issue #233).
REDIS_MAXMEMORY=256mb
REDIS_MAXMEMORY_POLICY=allkeys-lru
