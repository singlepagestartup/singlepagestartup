import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { api as hostLayoutsToWidgetsApi } from "@sps/host/relations/layouts-to-widgets/sdk/server";
import { insertSchema as hostLayoutsToWidgetsInsertSchema } from "@sps/host/relations/layouts-to-widgets/sdk/model";
import { RBAC_SECRET_KEY } from "@sps/shared-utils";

export function registerResources(mcp: McpServer) {
  mcp.registerResource(
    "host-relations-layouts-to-widgets",
    "sps://host/layouts-to-widgets",
    {
      title: "host layouts-to-widgets relation",
      description:
        "Get list of all layouts-to-widgets relations from host module",
    },
    async (uri) => {
      const resp = await hostLayoutsToWidgetsApi.find();

      return {
        contents: [
          {
            uri: uri.href,
            text: JSON.stringify(resp, null, 2),
          },
        ],
      };
    },
  );
}

export function registerTools(mcp: McpServer) {
  mcp.registerTool(
    "host-layouts-to-widgets-get",
    {
      title: "List of host layouts-to-widgets relations",
      description:
        "Get list of all layouts-to-widgets relations from host module.",
      inputSchema: {},
    },
    async () => {
      try {
        if (!RBAC_SECRET_KEY) {
          throw new Error("RBAC_SECRET_KEY is not set");
        }

        const entities = await hostLayoutsToWidgetsApi.find({
          options: {
            headers: {
              "X-RBAC-SECRET-KEY": RBAC_SECRET_KEY,
            },
          },
        });

        return {
          content: [
            {
              type: "text",
              text: JSON.stringify(entities, null, 2),
            },
          ],
        };
      } catch (error) {
        return {
          content: [
            {
              type: "text",
              text: `Error: ${error}`,
            },
          ],
        };
      }
    },
  );

  mcp.registerTool(
    "host-layouts-to-widgets-get-by-id",
    {
      title: "Get host layouts-to-widgets relation by id",
      description: "Get a layouts-to-widgets relation by id.",
      inputSchema: {
        id: hostLayoutsToWidgetsInsertSchema.shape.id,
      },
    },
    async (args) => {
      try {
        if (!args.id) {
          throw new Error("id is required");
        }

        const entity = await hostLayoutsToWidgetsApi.findById({
          id: args.id,
        });

        return {
          content: [
            {
              type: "text",
              text: JSON.stringify(entity, null, 2),
            },
          ],
        };
      } catch (error) {
        return {
          content: [
            {
              type: "text",
              text: `Error: ${error}`,
            },
          ],
        };
      }
    },
  );

  mcp.registerTool(
    "host-layouts-to-widgets-post",
    {
      title: "Create host layouts-to-widgets relation",
      description:
        "Create a new layouts-to-widgets relation in the host module.",
      inputSchema: hostLayoutsToWidgetsInsertSchema.shape,
    },
    async (args) => {
      try {
        if (!RBAC_SECRET_KEY) {
          throw new Error("RBAC_SECRET_KEY is not set");
        }

        const entity = await hostLayoutsToWidgetsApi.create({
          data: args,
          options: {
            headers: {
              "X-RBAC-SECRET-KEY": RBAC_SECRET_KEY,
            },
          },
        });

        return {
          content: [
            {
              type: "text",
              text: JSON.stringify(entity, null, 2),
            },
          ],
        };
      } catch (error) {
        return {
          content: [
            {
              type: "text",
              text: `Error: ${error}`,
            },
          ],
        };
      }
    },
  );

  mcp.registerTool(
    "host-layouts-to-widgets-patch",
    {
      title: "Update host layouts-to-widgets relation by id",
      description: "Update an existing layouts-to-widgets relation by id.",
      inputSchema: hostLayoutsToWidgetsInsertSchema.shape,
    },
    async (args) => {
      try {
        if (!args.id) {
          throw new Error("id is required for update");
        }

        if (!RBAC_SECRET_KEY) {
          throw new Error("RBAC_SECRET_KEY is not set");
        }

        const entity = await hostLayoutsToWidgetsApi.update({
          id: args.id,
          data: args,
          options: {
            headers: {
              "X-RBAC-SECRET-KEY": RBAC_SECRET_KEY,
            },
          },
        });

        return {
          content: [
            {
              type: "text",
              text: JSON.stringify(entity, null, 2),
            },
          ],
        };
      } catch (error) {
        return {
          content: [
            {
              type: "text",
              text: `Error: ${error}`,
            },
          ],
        };
      }
    },
  );

  mcp.registerTool(
    "host-layouts-to-widgets-delete",
    {
      title: "Delete host layouts-to-widgets relation by id",
      description: "Delete an existing layouts-to-widgets relation by id.",
      inputSchema: hostLayoutsToWidgetsInsertSchema.shape,
    },
    async (args) => {
      try {
        if (!args.id) {
          throw new Error("id is required for delete");
        }

        if (!RBAC_SECRET_KEY) {
          throw new Error("RBAC_SECRET_KEY is not set");
        }

        const entity = await hostLayoutsToWidgetsApi.delete({
          id: args.id,
          options: {
            headers: {
              "X-RBAC-SECRET-KEY": RBAC_SECRET_KEY,
            },
          },
        });

        return {
          content: [
            {
              type: "text",
              text: JSON.stringify(entity, null, 2),
            },
          ],
        };
      } catch (error) {
        return {
          content: [
            {
              type: "text",
              text: `Error: ${error}`,
            },
          ],
        };
      }
    },
  );
}
