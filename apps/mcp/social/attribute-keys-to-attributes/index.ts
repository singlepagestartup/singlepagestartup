import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { api as socialAttributeKeysToAttributesApi } from "@sps/social/relations/attribute-keys-to-attributes/sdk/server";
import { insertSchema as socialAttributeKeysToAttributesInsertSchema } from "@sps/social/relations/attribute-keys-to-attributes/sdk/model";
import { RBAC_SECRET_KEY } from "@sps/shared-utils";

export function registerResources(mcp: McpServer) {
  mcp.registerResource(
    "social-relations-attribute-keys-to-attributes",
    "sps://social/attribute-keys-to-attributes",
    {
      title: "social attribute-keys-to-attributes relation",
      description:
        "Get list of all attribute-keys-to-attributes relations from social module",
    },
    async (uri) => {
      const resp = await socialAttributeKeysToAttributesApi.find();

      return {
        contents: [
          {
            uri: uri.href,
            text: JSON.stringify(resp, null, 2),
          },
        ],
      };
    },
  );
}

export function registerTools(mcp: McpServer) {
  mcp.registerTool(
    "social-attribute-keys-to-attributes-get",
    {
      title: "List of social attribute-keys-to-attributes relations",
      description:
        "Get list of all attribute-keys-to-attributes relations from social module.",
      inputSchema: {},
    },
    async () => {
      try {
        if (!RBAC_SECRET_KEY) {
          throw new Error("RBAC_SECRET_KEY is not set");
        }

        const entities = await socialAttributeKeysToAttributesApi.find({
          options: {
            headers: {
              "X-RBAC-SECRET-KEY": RBAC_SECRET_KEY,
            },
          },
        });

        return {
          content: [
            {
              type: "text",
              text: JSON.stringify(entities, null, 2),
            },
          ],
        };
      } catch (error) {
        return {
          content: [
            {
              type: "text",
              text: `Error: ${error}`,
            },
          ],
        };
      }
    },
  );

  mcp.registerTool(
    "social-attribute-keys-to-attributes-get-by-id",
    {
      title: "Get social attribute-keys-to-attributes relation by id",
      description: "Get a attribute-keys-to-attributes relation by id.",
      inputSchema: {
        id: socialAttributeKeysToAttributesInsertSchema.shape.id,
      },
    },
    async (args) => {
      try {
        if (!args.id) {
          throw new Error("id is required");
        }

        const entity = await socialAttributeKeysToAttributesApi.findById({
          id: args.id,
        });

        return {
          content: [
            {
              type: "text",
              text: JSON.stringify(entity, null, 2),
            },
          ],
        };
      } catch (error) {
        return {
          content: [
            {
              type: "text",
              text: `Error: ${error}`,
            },
          ],
        };
      }
    },
  );

  mcp.registerTool(
    "social-attribute-keys-to-attributes-post",
    {
      title: "Create social attribute-keys-to-attributes relation",
      description:
        "Create a new attribute-keys-to-attributes relation in the social module.",
      inputSchema: socialAttributeKeysToAttributesInsertSchema.shape,
    },
    async (args) => {
      try {
        if (!RBAC_SECRET_KEY) {
          throw new Error("RBAC_SECRET_KEY is not set");
        }

        const entity = await socialAttributeKeysToAttributesApi.create({
          data: args,
          options: {
            headers: {
              "X-RBAC-SECRET-KEY": RBAC_SECRET_KEY,
            },
          },
        });

        return {
          content: [
            {
              type: "text",
              text: JSON.stringify(entity, null, 2),
            },
          ],
        };
      } catch (error) {
        return {
          content: [
            {
              type: "text",
              text: `Error: ${error}`,
            },
          ],
        };
      }
    },
  );

  mcp.registerTool(
    "social-attribute-keys-to-attributes-patch",
    {
      title: "Update social attribute-keys-to-attributes relation by id",
      description:
        "Update an existing attribute-keys-to-attributes relation by id.",
      inputSchema: socialAttributeKeysToAttributesInsertSchema.shape,
    },
    async (args) => {
      try {
        if (!args.id) {
          throw new Error("id is required for update");
        }

        if (!RBAC_SECRET_KEY) {
          throw new Error("RBAC_SECRET_KEY is not set");
        }

        const entity = await socialAttributeKeysToAttributesApi.update({
          id: args.id,
          data: args,
          options: {
            headers: {
              "X-RBAC-SECRET-KEY": RBAC_SECRET_KEY,
            },
          },
        });

        return {
          content: [
            {
              type: "text",
              text: JSON.stringify(entity, null, 2),
            },
          ],
        };
      } catch (error) {
        return {
          content: [
            {
              type: "text",
              text: `Error: ${error}`,
            },
          ],
        };
      }
    },
  );

  mcp.registerTool(
    "social-attribute-keys-to-attributes-delete",
    {
      title: "Delete social attribute-keys-to-attributes relation by id",
      description:
        "Delete an existing attribute-keys-to-attributes relation by id.",
      inputSchema: socialAttributeKeysToAttributesInsertSchema.shape,
    },
    async (args) => {
      try {
        if (!args.id) {
          throw new Error("id is required for delete");
        }

        if (!RBAC_SECRET_KEY) {
          throw new Error("RBAC_SECRET_KEY is not set");
        }

        const entity = await socialAttributeKeysToAttributesApi.delete({
          id: args.id,
          options: {
            headers: {
              "X-RBAC-SECRET-KEY": RBAC_SECRET_KEY,
            },
          },
        });

        return {
          content: [
            {
              type: "text",
              text: JSON.stringify(entity, null, 2),
            },
          ],
        };
      } catch (error) {
        return {
          content: [
            {
              type: "text",
              text: `Error: ${error}`,
            },
          ],
        };
      }
    },
  );
}
