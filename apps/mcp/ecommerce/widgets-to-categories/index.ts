import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { api as ecommerceWidgetsToCategoriesApi } from "@sps/ecommerce/relations/widgets-to-categories/sdk/server";
import { insertSchema as ecommerceWidgetsToCategoriesInsertSchema } from "@sps/ecommerce/relations/widgets-to-categories/sdk/model";
import { RBAC_SECRET_KEY } from "@sps/shared-utils";

export function registerResources(mcp: McpServer) {
  mcp.registerResource(
    "ecommerce-relations-widgets-to-categories",
    "sps://ecommerce/widgets-to-categories",
    {
      title: "ecommerce widgets-to-categories relation",
      description:
        "Get list of all widgets-to-categories relations from ecommerce module",
    },
    async (uri) => {
      const resp = await ecommerceWidgetsToCategoriesApi.find();

      return {
        contents: [
          {
            uri: uri.href,
            text: JSON.stringify(resp, null, 2),
          },
        ],
      };
    },
  );
}

export function registerTools(mcp: McpServer) {
  mcp.registerTool(
    "ecommerce-widgets-to-categories-get",
    {
      title: "List of ecommerce widgets-to-categories relations",
      description:
        "Get list of all widgets-to-categories relations from ecommerce module.",
      inputSchema: {},
    },
    async () => {
      try {
        if (!RBAC_SECRET_KEY) {
          throw new Error("RBAC_SECRET_KEY is not set");
        }

        const entities = await ecommerceWidgetsToCategoriesApi.find({
          options: {
            headers: {
              "X-RBAC-SECRET-KEY": RBAC_SECRET_KEY,
            },
          },
        });

        return {
          content: [
            {
              type: "text",
              text: JSON.stringify(entities, null, 2),
            },
          ],
        };
      } catch (error) {
        return {
          content: [
            {
              type: "text",
              text: `Error: ${error}`,
            },
          ],
        };
      }
    },
  );

  mcp.registerTool(
    "ecommerce-widgets-to-categories-get-by-id",
    {
      title: "Get ecommerce widgets-to-categories relation by id",
      description: "Get a widgets-to-categories relation by id.",
      inputSchema: {
        id: ecommerceWidgetsToCategoriesInsertSchema.shape.id,
      },
    },
    async (args) => {
      try {
        if (!args.id) {
          throw new Error("id is required");
        }

        const entity = await ecommerceWidgetsToCategoriesApi.findById({
          id: args.id,
        });

        return {
          content: [
            {
              type: "text",
              text: JSON.stringify(entity, null, 2),
            },
          ],
        };
      } catch (error) {
        return {
          content: [
            {
              type: "text",
              text: `Error: ${error}`,
            },
          ],
        };
      }
    },
  );

  mcp.registerTool(
    "ecommerce-widgets-to-categories-post",
    {
      title: "Create ecommerce widgets-to-categories relation",
      description:
        "Create a new widgets-to-categories relation in the ecommerce module.",
      inputSchema: ecommerceWidgetsToCategoriesInsertSchema.shape,
    },
    async (args) => {
      try {
        if (!RBAC_SECRET_KEY) {
          throw new Error("RBAC_SECRET_KEY is not set");
        }

        const entity = await ecommerceWidgetsToCategoriesApi.create({
          data: args,
          options: {
            headers: {
              "X-RBAC-SECRET-KEY": RBAC_SECRET_KEY,
            },
          },
        });

        return {
          content: [
            {
              type: "text",
              text: JSON.stringify(entity, null, 2),
            },
          ],
        };
      } catch (error) {
        return {
          content: [
            {
              type: "text",
              text: `Error: ${error}`,
            },
          ],
        };
      }
    },
  );

  mcp.registerTool(
    "ecommerce-widgets-to-categories-patch",
    {
      title: "Update ecommerce widgets-to-categories relation by id",
      description: "Update an existing widgets-to-categories relation by id.",
      inputSchema: ecommerceWidgetsToCategoriesInsertSchema.shape,
    },
    async (args) => {
      try {
        if (!args.id) {
          throw new Error("id is required for update");
        }

        if (!RBAC_SECRET_KEY) {
          throw new Error("RBAC_SECRET_KEY is not set");
        }

        const entity = await ecommerceWidgetsToCategoriesApi.update({
          id: args.id,
          data: args,
          options: {
            headers: {
              "X-RBAC-SECRET-KEY": RBAC_SECRET_KEY,
            },
          },
        });

        return {
          content: [
            {
              type: "text",
              text: JSON.stringify(entity, null, 2),
            },
          ],
        };
      } catch (error) {
        return {
          content: [
            {
              type: "text",
              text: `Error: ${error}`,
            },
          ],
        };
      }
    },
  );

  mcp.registerTool(
    "ecommerce-widgets-to-categories-delete",
    {
      title: "Delete ecommerce widgets-to-categories relation by id",
      description: "Delete an existing widgets-to-categories relation by id.",
      inputSchema: ecommerceWidgetsToCategoriesInsertSchema.shape,
    },
    async (args) => {
      try {
        if (!args.id) {
          throw new Error("id is required for delete");
        }

        if (!RBAC_SECRET_KEY) {
          throw new Error("RBAC_SECRET_KEY is not set");
        }

        const entity = await ecommerceWidgetsToCategoriesApi.delete({
          id: args.id,
          options: {
            headers: {
              "X-RBAC-SECRET-KEY": RBAC_SECRET_KEY,
            },
          },
        });

        return {
          content: [
            {
              type: "text",
              text: JSON.stringify(entity, null, 2),
            },
          ],
        };
      } catch (error) {
        return {
          content: [
            {
              type: "text",
              text: `Error: ${error}`,
            },
          ],
        };
      }
    },
  );
}
